# NPB_SP_cpu_parallel_homework_2

Данный репозиторий содержит выполненное задание по распараллеливанию теста **SP** из набора **NAS Parallel Benchmarks (NPB 3.3, C-версия)** с использованием модели **C-DVMH** для многопроцессорных систем с общей памятью.

---

## 📌 1. Постановка задачи

DVM-система предназначена для разработки параллельных программ научно-технических расчётов на языках **C-DVMH** и **Fortran-DVMH**.  
Набор **NAS Parallel Benchmarks (NPB)** широко используется для оценки производительности различных систем параллельного программирования.

Однако **тест SP на C ещё не был распараллелен в модели DVMH**, поэтому целью данной работы является:

---

## 🎯 2. Цель работы

- Выполнить **распараллеливание теста SP** (C-версии NPB) с использованием языка **C-DVMH**.  
- Использовать **модель общей памяти мультипроцессора** (без распределения данных, без GPU).  
- Оценить **эффективность полученной параллельной версии** (ускорение, масштабируемость).  
- Освоить применение директив:
  - `#pragma dvm region`
  - `#pragma dvm parallel(<число>)`
  - `reduction`, `private`

---

## 🧩 3. Подход к распараллеливанию

Работа выполнялась **инкрементально**, то есть:

1. Последовательно находились наиболее трудоёмкие циклы.
2. На каждый этап добавлялись:
   - `region`
   - `parallel(<число>)`
3. Проверялась корректность вычислений.
4. Проводились измерения времени.

Такой подход соответствует предложенной методике:  
начать с одного цикла → затем расширять параллельные регионы.

---

## 🗂 4. Структура репозитория

npb-3.3-c/
          SP/          ← модифицированные файлы теста SP (CPU DVMH)
          BT/
          LU/
          …
          common/
          config/
          sys/
          README.md

---

## ✏️ 5. Изменённые файлы (SP)

Параллелизация выполнена в следующих файлах модуля SP:

- `add.c`
- `error.c`
- `exact_rhs.c`
- `initialize.c`
- `ninvr.c`
- `pinvr.c`
- `rhs.c`
- `txinvr.c`
- `tzetar.c`
- `x_solve.c`
- `y_solve.c`
- `z_solve.c`

В эти файлы добавлены:
- DVMH-регионы  
- директивы `parallel(<число>)`  
- спецификации `private`, `reduction`

Цель — распараллелить вложенные циклы, сохранив корректность SP.

---

## ⚙️ 6. Компиляция и запуск

Компиляция исходной программы SP выполняется с помощью стандартного Makefile, находящегося в каталоге `SP`.

### 🔧 6.1. Компиляция

Перейдите в каталог:
(пример CLASS=A)(CLASS можно равно A, B, C, S)

```bash
cd npb-3.3-c/SP
make CLASS=A ACTION=exec 
```
Параметры запуск можно задавать редактируя сам скрипт dvm, основное внимание стоит уделить переменным DVMH_NUM_THREADS (на этапе 2)
```bash
DVMH_NUM_THREADS=1..N
./dvm run ../bin/sp.A.x
```
Для удаления промежуточных файлов и повторной чистой сборки используйте:
```bash
make clean
```

---

## 📊 7. Результаты

Итоговые результаты выполнения оптимизированной параллельной версии теста SP для различных классов задач представлены в следующих файлах:

- `npb-3.3-c/result_A.txt` — результаты для класса A  
- `npb-3.3-c/result_C.txt` — результаты для класса C  

Файлы содержат время выполнения программы при различном количестве потоков(cpu = 1, 2, 4, 8), а также рассчитанное ускорение.

---
